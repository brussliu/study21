# アイコン（占位说明）

本目录用于存放 PWA 图标。当前 `icon.svg` 为占位图标。

正式发布前请替换为以下尺寸的 PNG 图标（建议），并同步更新 `manifest.webmanifest` 的 `icons` 配置：

- icon-192.png（192×192）
- icon-512.png（512×512）
- apple-touch-icon.png（180×180，可选）

本阶段不生成复杂图标，仅保留占位说明。
