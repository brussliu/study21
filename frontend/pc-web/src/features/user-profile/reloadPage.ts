/**
 * ダイアログで保存したあとに親ページを読み込み直す。
 *
 * 「ユーザー情報の修正」「パスワードの変更」はページ遷移ではなくダイアログで行い、
 * 保存後は親ページ（一覧やホームなど、開いていた画面）をそのまま再読み込みして
 * 変更内容を反映させる。
 *
 * テストから差し替えられるよう、ブラウザ API の呼び出しを 1 か所にまとめている。
 */
export function reloadParentPage(): void {
  window.location.reload()
}
